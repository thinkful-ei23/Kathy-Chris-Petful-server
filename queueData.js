'use strict'

const Queue = require('./queue');

function peek(queue) { //works
	let currentNode = queue.top;
	if (currentNode === null) {
		return null;
	}
	return currentNode.data
};


const catQueue = () => {
	cat.enqueue = cat.JSON



}